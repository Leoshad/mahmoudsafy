(()=>{'use strict';
let crown=null,dialog=null,lastRevision=null,observer=null;
const $=s=>document.querySelector(s),make=(tag,text,parent,cls)=>{const n=document.createElement(tag);n.textContent=text;if(cls)n.className=cls;parent?.append(n);return n;};
function details(){if(!crown)return;dialog?.remove();dialog=make('dialog','',document.body,'crown-dialog');const close=make('button','Close',dialog,'crown-close');close.onclick=()=>dialog.close();make('div','👑',dialog,'crown-emblem');make('small','SOLO CROWN · TRIAL',dialog,'crown-eyebrow');make('h2',crown.holder?crown.holder==='Mahmoud'?'Mahmoud · King of Games':'Computer holds the crown':'The crown is waiting',dialog);make('p',crown.holder?'Catch the leader. Take the crown.':'Win your first match to claim it.',dialog,'crown-muted');const table=make('table','',dialog);const head=make('tr','',table);for(const t of ['Game','Mahmoud','Computer'])make('th',t,head);for(const [key,title]of [['domino','Dominoes'],['ocho','Ocho'],['totals','Total wins']]){const row=make('tr','',table);make('th',title,row);for(const n of ['Mahmoud','Computer'])make('td',String((key==='totals'?crown.totals:crown.breakdown[key])[n]),row);}make('p','One completed match = one win. At a tie, the reigning champion keeps the crown until overtaken.',dialog,'crown-muted');make('p','Mahmoud vs Computer only. Games with Safy do not count in this trial.',dialog,'crown-muted');if(crown.events.length){make('h3','Crown moments',dialog);for(const e of crown.events.slice(0,5))make('p',e.holder+' took the crown · '+e.totals.Mahmoud+'–'+e.totals.Computer,dialog);}dialog.addEventListener('click',e=>{if(e.target===dialog){const r=dialog.getBoundingClientRect();if(e.clientX<r.left||e.clientX>r.right||e.clientY<r.top||e.clientY>r.bottom)dialog.close();}});dialog.showModal();}
function mark(node,active,avatar=false){if(!node)return;node.classList.toggle('crown-holder',!!active);node.classList.toggle('crown-avatar',!!active&&avatar);if(active){node.title='King of Games · Solo crown · '+crown.totals.Mahmoud+'–'+crown.totals.Computer;node.setAttribute('aria-label',node.title);if(!node.dataset.crownBound){node.dataset.crownBound='1';node.addEventListener('click',details);node.addEventListener('keydown',e=>{if(e.key==='Enter'||e.key===' '){e.preventDefault();details();}});}node.setAttribute('role','button');node.tabIndex=0;}else if(node.dataset.crownBound){node.removeAttribute('title');node.removeAttribute('aria-label');node.removeAttribute('role');node.removeAttribute('tabindex');}}
function decorate(){const mine=crown?.holder==='Mahmoud';for(const n of document.querySelectorAll('.message.mahmoud .avatar'))mark(n,mine,true);for(const n of document.querySelectorAll('#presence .person'))mark(n,mine&&n.textContent.trim().startsWith('Mahmoud'));for(const n of document.querySelectorAll('#account > span'))mark(n,mine);const ochoSolo=$('#ocho-solo')?.classList.contains('selected'),dominoSolo=$('#domino-solo-tab')?.classList.contains('selected');
 for(const [selector,solo] of [['#ocho-game .ocho-records',ochoSolo],['#domino-records',dominoSolo]]){const records=$(selector);if(!records)continue;const active=!!crown?.holder&&!!solo;records.classList.toggle('crown-game-records',active);records.dataset.crown=active?'👑 '+crown.holder+' · King of Games':'';}
}

function celebrate(){const event=crown.events[0];if(!event)return;$('#crown-moment')?.remove();const banner=make('div','',$('#chat'),'crown-moment');banner.id='crown-moment';banner.setAttribute('role','status');make('span',event.holder==='Mahmoud'?'M':'C',banner,'crown-moment-avatar');const body=make('div','',banner);make('strong',event.holder+' takes the crown 👑',body);make('small',event.totals.Mahmoud+'–'+event.totals.Computer+' · Solo rivalry',body);const close=make('button','×',banner);close.setAttribute('aria-label','Dismiss crown moment');close.onclick=()=>banner.remove();}
window.OurCrown={sync(s){const next=s.who==='Mahmoud'?s.crown:null;const changed=next&&lastRevision!==null&&next.revision>lastRevision;crown=next;lastRevision=next?.revision??null;const summary=$('#crown-summary');if(summary){summary.hidden=!crown;summary.replaceChildren();if(crown){make('span','👑',summary,'crown-summary-icon');const body=make('span','',summary);make('small','SOLO CROWN',body);make('strong',crown.holder?crown.holder==='Mahmoud'?'You hold the crown':'Computer holds the crown':'Your crown awaits',body);make('small',crown.totals.Mahmoud+' – '+crown.totals.Computer+' · Total match wins',body);make('span','›',summary);summary.onclick=details;}}decorate();if(changed)celebrate();if(!observer&&window.MutationObserver){observer=new MutationObserver(decorate);for(const selector of ['#feed','#presence','#account','#ocho-game','#domino-records']){const node=$(selector);if(node)observer.observe(node,{childList:true,subtree:true});}}},decorate,reset(){crown=null;lastRevision=null;observer?.disconnect();observer=null;dialog?.remove();dialog=null;$('#crown-moment')?.remove();if($('#crown-summary'))$('#crown-summary').hidden=true;decorate();}};
})();
